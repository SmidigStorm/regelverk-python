"""
Domain ports (interfaces) for infrastructure implementations.

Ports define what the domain needs from external systems without specifying how.
Infrastructure layer will implement these protocols.
"""
